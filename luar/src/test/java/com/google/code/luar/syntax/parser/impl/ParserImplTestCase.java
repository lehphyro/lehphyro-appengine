package com.google.code.luar.parser.impl;

//import net.java.luaj.syntax.lexer.impl.*;
//import net.java.luaj.syntax.parser.*;
//import net.java.luaj.syntax.tree.*;
import junit.framework.*;

public class ParserImplTestCase extends TestCase {
/* TODO implement
	public void testParseIfNode() {
		String program = "if 1 == 1 then print 'Yes' elseif 1 == 2 then print 'No' else print 'Maybe' end";
		Parser parser = new ParserImpl(new LexerImpl(program));
		ChunkNode node = parser.parse();
		IfNode ifNode = (IfNode)node.getStatements().get(0);

		IfNodePart firstPart = (IfNodePart)ifNode.getConditionParts().get(0);
		IfNodePart secondPart = (IfNodePart)ifNode.getConditionParts().get(1);
		BlockNode elsePart = ifNode.getElsePart();
	}
*/
}